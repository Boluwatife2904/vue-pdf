node_modules
dist
# pdfkit's rollup output only — a bare `lib` also swallowed
# packages/pdfkit/src/saslprep/lib, which is source.
packages/pdfkit/lib
*.log
.env
.DS_Store
coverage
implementations

# vite-plugin-monaco-editor writes worker bundles to an absolute path
# resolved against cwd, producing a nested Users/... tree at build time.
docs/Users
.vercel
docs/.vercel
.output
docs/.output
.nuxt
docs/.nuxt
.pnpm-store
docs/public/llms.txt
docs/public/llms-full.txt
